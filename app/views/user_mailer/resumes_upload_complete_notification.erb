Uploaded resumes for Job Fair: <%=  @jobfair.name -%> on <%=  Date.today %>.

Statistics:
		Total Resumes: <%=  @total_records %>
		Rejected: <%=  @total_rejected %>

Username and Passwords file location:
		<%= @passw_filename %>

<% unless @warnings.to_s.blank? %>
		Warnings:
		<% @warnings.each do |warning| %>
				<%= warning[:filename] -%>: <%= warning[:errors].collect {|e| e.entries.inspect}.join(', ') %>
		<% end %>
<% end %>